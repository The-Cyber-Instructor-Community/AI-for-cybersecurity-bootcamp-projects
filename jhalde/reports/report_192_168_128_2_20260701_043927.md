# AutoRedTeam Pentest Report

**Target:** 192.168.128.2
**Date:** 2026-07-01
**Assessed By:** AutoRedTeam AI Agent

---

## Executive Summary

The target host (192.168.128.2, identified as **Metasploitable 2**) presents an extremely high-risk security posture with **12 open ports** exposing a wide range of critically vulnerable and legacy services. Multiple services are running software with well-known, publicly documented remote code execution vulnerabilities — including a backdoored FTP daemon (vsftpd 2.3.4) and an unauthenticated command injection flaw in Samba 3.0.20 — either of which alone would result in full system compromise. SMB enumeration revealed **35 user accounts** and a world-readable network share, significantly expanding the attack surface for credential-based attacks and lateral movement. Immediate remediation or decommissioning of this host is strongly advised.

---

## Findings

---

### [CRITICAL] vsftpd 2.3.4 Backdoor — Remote Code Execution

- **Service:** Port 21/tcp — vsftpd 2.3.4
- **CVE:** CVE-2011-2523 (CVSS 10.0)
- **Description:** vsftpd version 2.3.4 was distributed with a deliberate backdoor introduced into its source code. When a username containing the string `:)` (smiley face) is submitted during login, the daemon opens a bind shell on **TCP port 6200**, granting an unauthenticated remote attacker a root-level interactive shell with no credentials required. This is one of the most well-known supply-chain compromises in open-source history.
- **Evidence:** Nmap confirmed `vsftpd 2.3.4` running on port 21. This exact version string is the sole indicator needed to confirm the presence of the backdoor.
- **Recommendation:** Immediately disable and remove vsftpd 2.3.4. Replace with a patched, actively maintained FTP server (e.g., vsftpd 3.0.5+). If FTP functionality is not required, disable the service entirely and block port 21 at the firewall. Consider replacing FTP with SFTP (port 22) for secure file transfer.
- **ATT&CK:** T1210 — Exploitation of Remote Services

---

### [CRITICAL] Samba 3.0.20 — Remote Code Execution via Username Map Script (CVE-2007-2447)

- **Service:** Port 445/tcp — Samba smbd 3.0.20-Debian
- **CVE:** CVE-2007-2447 (CVSS 9.3)
- **Description:** Samba versions 3.0.0 through 3.0.25rc3 are vulnerable to an unauthenticated remote command injection flaw in the `MS-RPC` `SamrChangePassword` function when the `username map script` configuration option is enabled. An attacker can inject arbitrary shell metacharacters into the username field, which are executed as the `root` user by the Samba daemon, resulting in full system compromise without any valid credentials.
- **Evidence:** enum4linux confirmed `Samba 3.0.20-Debian` on ports 139 and 445. The IPC share (`IPC$`) is accessible and the banner explicitly identifies the vulnerable version: *"metasploitable server (Samba 3.0.20-Debian)"*.
- **Recommendation:** Upgrade Samba immediately to a supported release (4.18+). Disable the `username map script` directive in `smb.conf` if present. Restrict SMB access (ports 139/445) to trusted internal hosts only using firewall rules. Consider disabling SMB entirely if file sharing is not required.
- **ATT&CK:** T1210 — Exploitation of Remote Services

---

### [CRITICAL] SMB User Enumeration — 35 Accounts Exposed via Null-Session-Adjacent RID Cycling

- **Service:** Port 139/445/tcp — Samba smbd 3.0.20-Debian
- **CVE:** N/A — CWE-200 (Information Exposure)
- **Description:** The Samba service permitted full enumeration of **35 system user accounts** via RID cycling during unauthenticated or minimally authenticated SMB queries. The enumerated accounts include high-value targets such as `root`, `msfadmin`, `postgres`, `user`, `service`, `tomcat55`, and `mysql`. These usernames provide attackers with a definitive target list for password spraying, brute-force, and credential stuffing attacks across all network services (SSH, FTP, Telnet, etc.).
- **Evidence:** enum4linux returned 35 usernames including: `root` (RID 1000), `msfadmin` (RID 3000), `user` (RID 3002), `postgres` (RID 1216), `mysql` (RID 1218), `tomcat55` (RID 1220), `service` (RID 3004), and 28 additional accounts.
- **Recommendation:** Restrict SMB enumeration by setting `restrict anonymous = 2` in `smb.conf`. Enforce strong, unique passwords on all accounts. Disable unused system accounts. Implement account lockout policies and monitor for repeated authentication failures across SSH, FTP, and Telnet.
- **ATT&CK:** T1110.003 — Password Spraying / T1110 — Brute Force

---

### [CRITICAL] Exposed Writable SMB Share — `tmp`

- **Service:** Port 445/tcp — Samba smbd 3.0.20-Debian (Share: `tmp`)
- **CVE:** N/A — CWE-732 (Incorrect Permission Assignment for Critical Resource)
- **Description:** The SMB share named `tmp` (commented *"oh noes!"*) is publicly accessible with both **read and write** permissions, requiring no authentication. An attacker can upload arbitrary files — including malicious executables or scripts — directly to the server's filesystem. Combined with other vulnerabilities on this host, this share can serve as a staging ground for payload delivery and persistence.
- **Evidence:** enum4linux reported `tmp` share with `mapping: ok` and `listing: ok`, confirming unauthenticated read/write access. The sarcastic comment *"oh noes!"* in the share description indicates the administrator was aware of this misconfiguration.
- **Recommendation:** Immediately remove or restrict the `tmp` share. If a temporary share is operationally required, enforce authentication, apply read-only permissions, and restrict access to specific IP addresses via `hosts allow` in `smb.conf`. Audit all files currently present in the share.
- **ATT&CK:** T1210 — Exploitation of Remote Services / T1071.002 — File Transfer Protocols

---

### [HIGH] Telnet Service Enabled — Cleartext Credential Transmission (Port 23)

- **Service:** Port 23/tcp — Linux telnetd
- **CVE:** CVE-2002-0628 (CVSS 7.5) / General CWE-319 (Cleartext Transmission of Sensitive Information)
- **Description:** Telnet transmits all data — including usernames, passwords, and session content — in **plaintext** over the network. Any attacker with the ability to intercept network traffic (ARP spoofing, MITM, passive sniffing) can trivially capture valid credentials. Given the 35 enumerated usernames on this host, Telnet represents a high-probability credential harvesting vector. Telnet also provides no host authentication, making it vulnerable to server spoofing.
- **Evidence:** Nmap confirmed `Linux telnetd` listening on port 23/tcp. Combined with 35 enumerated usernames, this service is a prime target for both credential capture and brute-force login attempts.
- **Recommendation:** Immediately disable Telnet (`systemctl disable telnetd`, block port 23 at the firewall). Replace all Telnet-based administrative access with **SSH** (port 22 is already open on this host). Audit logs for any existing Telnet sessions.
- **ATT&CK:** T1110.001 — Password Guessing / T1040 — Network Sniffing

---

### [HIGH] OpenSSH 4.7p1 — Signal Handler Race Condition (CVE-2006-5051)

- **Service:** Port 22/tcp — OpenSSH 4.7p1 Debian 8ubuntu1 (protocol 2.0)
- **CVE:** CVE-2006-5051 (CVSS 8.1)
- **Description:** OpenSSH versions prior to 4.4 contain a signal handler race condition that can cause a crash (denial of service) and, if GSSAPI authentication is enabled, may allow remote attackers to execute arbitrary code via a double-free memory corruption vulnerability. While exploitation is non-trivial, the version in use is severely outdated (released ~2007) and may be subject to additional undisclosed vulnerabilities. The version is also older than the patch for CVE-2015-8325, which allows local privilege escalation when `UseLogin` is enabled.
- **Evidence:** Nmap banner: `OpenSSH 4.7p1 Debian 8ubuntu1 (protocol 2.0)` on port 22/tcp.
- **Recommendation:** Upgrade OpenSSH to the latest stable release (9.x+). Disable GSSAPI authentication if not in use (`GSSAPIAuthentication no` in `sshd_config`). Enforce key-based authentication only and disable password authentication. Restrict SSH access to known management IP addresses via firewall rules.
- **ATT&CK:** T1210 — Exploitation of Remote Services

---

### [HIGH] ISC BIND 9.4.2 — Multiple Denial of Service Vulnerabilities

- **Service:** Port 53/tcp — ISC BIND 9.4.2
- **CVE:** CVE-2016-1286 (CVSS 8.6)
- **Description:** ISC BIND 9.4.2 is a severely outdated DNS server (released ~2007, end-of-life). Numerous CVEs exist across this version lineage, including assertion failures that cause daemon crashes leading to denial of service (CVE-2016-1286). Furthermore, having DNS exposed on TCP port 53 to untrusted networks may allow **DNS zone transfer** attacks, enabling an attacker to harvest all DNS records (hostnames, IP addresses, and network topology) if zone transfers are not restricted.
- **Evidence:** Nmap confirmed `ISC BIND 9.4.2` on port 53/tcp. This version is approximately 12+ major releases behind current stable.
- **Recommendation:** Upgrade BIND to the latest stable release (9.18+ LTS). Restrict zone transfers to authorised secondary DNS servers only (`allow-transfer` directive). Restrict port 53 TCP/UDP access to authorised resolvers and DNS clients only. Consider migrating to a modern DNS resolver (Unbound, PowerDNS).
- **ATT&CK:** T1590.002 — DNS / T1498 — Network Denial of Service

---

### [HIGH] R-Services Enabled — rexec / rlogin / rsh (Ports 512/513/514)

- **Service:** Ports 512/tcp (rexecd), 513/tcp (rlogind), 514/tcp (rshd) — Netkit R-Services
- **CVE:** CWE-287 — Improper Authentication / CVE class: inherently insecure-by-design protocols
- **Description:** The legacy BSD R-Services (`rexec`, `rlogin`, `rsh`) provide remote command execution and login functionality with **no encryption** and **host-based trust authentication** using `.rhosts` and `hosts.equiv` files. These services are considered inherently insecure: they transmit credentials in plaintext, rely on easily spoofed IP-based authentication, and are a well-known target for exploitation. If a `.rhosts` file contains `+ +`, any host can authenticate as any user without a password.
- **Evidence:** Nmap confirmed `netkit-rsh rexecd` on port 512, `rlogind` on port 513, and `Netkit rshd` on port 514 — all open and listening.
- **Recommendation:** Immediately disable all R-Services (`rexec`, `rlogin`, `rsh`). Remove or purge the `rsh-server` and `rlogin` packages. Block ports 512, 513, and 514 at the firewall. Replace with SSH for all remote access. Audit and remove any `.rhosts` or `hosts.equiv` files on the system.
- **ATT&CK:** T1021.004 — Remote Services: SSH / T1078 — Valid Accounts

---

### [MEDIUM] Apache httpd 2.2.8 with WebDAV Enabled — Potential Unauthorised File Upload

- **Service:** Port 80/tcp — Apache httpd 2.2.8 (Ubuntu) DAV/2
- **CVE:** CVE-2015-3184 / CWE-284 — Improper Access Control (WebDAV)
- **Description:** Apache 2.2.8 is an end-of-life web server version (mainstream support ended 2013). The presence of `DAV/2` in the banner indicates **WebDAV is enabled**, which, if not properly restricted, allows unauthenticated clients to upload, modify, or delete files on the web server. Additionally, outdated Apache 2.2.x versions are subject to numerous known vulnerabilities. Gobuster enumeration returned no discoverable paths, which may indicate a minimal configuration or non-standard wordlist coverage.
- **Evidence:** Nmap banner: `Apache httpd 2.2.8 ((Ubuntu) DAV/2)` on port 80/tcp. Gobuster returned 0 paths. Nikto scan timed out (service may have rate-limiting or anomalous response behaviour).
- **Recommendation:** Upgrade Apache to the latest stable release (2.4.x). Disable WebDAV if not explicitly required (`a2dismod dav dav_fs`). Implement authentication on all WebDAV-enabled directories. Apply security headers (X-Frame-Options, Content-Security-Policy, X-Content-Type-Options). Restrict HTTP access to authorised sources.
- **ATT&CK:** T1190 — Exploit Public-Facing Application

---

### [MEDIUM] Postfix SMTP — User Enumeration via VRFY/EXPN Commands

- **Service:** Port 25/tcp — Postfix smtpd
- **CVE:** CWE-200 — Information Exposure
- **Description:** SMTP servers that respond to `VRFY` and `EXPN` commands allow attackers to enumerate valid system user accounts without authentication. Given the 35 usernames already enumerated via SMB, SMTP VRFY/EXPN could independently confirm valid mail-receiving accounts and further facilitate targeted phishing, credential attacks, or spam relay abuse. Open relay configuration, if present, would additionally allow the server to be weaponised for spam campaigns.
- **Evidence:** Nmap confirmed `Postfix smtpd` on port 25/tcp. No authentication challenge was observed on the banner, suggesting anonymous connections are permitted for initial SMTP dialogue.
- **Recommendation:** Disable VRFY and EXPN commands in Postfix (`disable_vrfy_command = yes` in `main.cf`). Configure Postfix to reject open relay (`smtpd_recipient_restrictions`). Restrict SMTP access to legitimate mail sources. Enable SMTP authentication (SASL) for outbound relay.
- **ATT&CK:** T1590.001 — Gather Victim Network Information / T1078 — Valid Accounts

---

### [MEDIUM] RPC Portmapper Exposed — Service Discovery and Attack Surface Expansion (Port 111)

- **Service:** Port 111/tcp — rpcbind 2 (RPC #100000)
- **CVE:** CVE-2015-7236 (CVSS 7.5)
- **Description:** The `rpcbind` (portmapper) service on port 111 maps RPC program numbers to network port numbers, effectively acting as a **service directory** for all RPC-based services. An unauthenticated attacker can query rpcbind to enumerate all active RPC services on the host (NFS, NIS, mountd, etc.), dramatically expanding the known attack surface. Additionally, CVE-2015-7236 describes a use-after-free vulnerability in rpcbind 0.2.1 and earlier that can cause a daemon crash via crafted packets.
- **Evidence:** Nmap confirmed `rpcbind 2 (RPC #100000)` listening on port 111/tcp, openly accessible with no authentication required.
- **Recommendation:** Restrict access to port 111 via firewall rules to trusted internal hosts only. If NFS or other RPC-dependent services are not required, disable rpcbind entirely (`systemctl disable rpcbind`). Upgrade rpcbind to the latest available version.
- **ATT&CK:** T1046 — Network Service Discovery

---

## Risk Summary

| Severity | Count |
|----------|-------|
| CRITICAL | 4 |
| HIGH | 4 |
| MEDIUM | 3 |
| LOW | 0 |

**Total Findings: 11**

---

## Remediation Priority

1. **🔴 Immediately patch or remove vsftpd 2.3.4** — The backdoor on port 21 grants unauthenticated root shell access in seconds. This is a zero-effort, maximum-impact compromise vector.
2. **🔴 Upgrade Samba from 3.0.20 to a supported release** — CVE-2007-2447 provides unauthenticated RCE as root via a single malformed SMB request. Restrict ports 139/445 at the perimeter immediately as an interim measure.
3. **🔴 Remove world-readable/writable `tmp` SMB share** — The anonymous write-accessible share allows direct file staging on the target filesystem with no authentication.
4. **🔴 Disable Telnet and all R-Services (ports 23, 512, 513, 514)** — These services transmit credentials in cleartext and use trivially bypassable host-based authentication. All remote access must migrate exclusively to SSH.
5. **🔱 Restrict SMB user enumeration** — Set `restrict anonymous = 2` in `smb.conf` and enforce strong passwords and lockout policies across all 35 enumerated accounts to prevent credential attacks.
6. **🟠 Upgrade OpenSSH from 4.7p1 to 9.x+** — Disable password authentication, enforce key-based login, and restrict SSH to management IP addresses only.
7. **🟠 Upgrade ISC BIND 9.4.2 and restrict DNS zone transfers** — Replace with a supported DNS resolver; restrict TCP/53 access to authorised hosts only.
8. **🟡 Upgrade Apache 2.2.8 and disable WebDAV** — Replace with Apache 2.4.x, remove DAV modules unless explicitly required, and apply security hardening headers.
9. **🟡 Harden Postfix SMTP** — Disable VRFY/EXPN commands and enforce anti-relay restrictions to prevent user enumeration and mail abuse.
10. **🟡 Restrict or disable rpcbind on port 111** — Firewall port 111 from untrusted networks and disable all unused RPC-dependent services (NFS, NIS, mountd).

---
*Report generated by AutoRedTeam AI Agent | Assessment Date: 2026-07-01 | Target: 192.168.128.2*

## Remediation Playbook

---

### Fix: vsftpd 2.3.4 Backdoor — Remote Code Execution

**Severity:** CRITICAL  
**Effort:** Quick

**Remediation Commands:**
```bash
# Stop and disable vsftpd service
sudo systemctl stop vsftpd
sudo systemctl disable vsftpd

# Remove vsftpd package
sudo apt-get remove -y vsftpd
sudo apt-get purge -y vsftpd

# Block port 21 at the firewall (if UFW is enabled)
sudo ufw deny 21/tcp

# If FTP functionality is required, install a patched alternative
# sudo apt-get install -y vsftpd=3.0.5* (or latest available)
# For secure file transfer, verify SSH/SFTP is enabled instead:
sudo systemctl enable ssh
sudo systemctl start ssh
```

**Verification Commands:**
```bash
# Verify service is stopped and disabled
sudo systemctl status vsftpd 2>&1 | grep -i "inactive\|disabled"

# Verify package is removed
dpkg -l | grep vsftpd || echo "vsftpd successfully removed"

# Verify port 21 is not listening
sudo netstat -tlnp | grep ':21' || echo "Port 21 is not listening"
sudo ss -tlnp | grep ':21' || echo "Port 21 confirmed closed"

# Verify SSH/SFTP is available as alternative
sudo systemctl is-active ssh
sudo netstat -tlnp | grep ':22'
```

---

### Fix: Samba 3.0.20 — Remote Code Execution via Username Map Script (CVE-2007-2447)

**Severity:** CRITICAL  
**Effort:** Medium

**Remediation Commands:**
```bash
# Stop Samba services
sudo systemctl stop smbd
sudo systemctl stop nmbd
sudo systemctl disable smbd
sudo systemctl disable nmbd

# Backup current smb.conf
sudo cp /etc/samba/smb.conf /etc/samba/smb.conf.backup.$(date +%s)

# Remove vulnerable version
sudo apt-get remove -y samba samba-common
sudo apt-get purge -y samba samba-common

# Update package lists and install latest stable Samba
sudo apt-get update
sudo apt-get install -y samba samba-common samba-dsdb-modules samba-vfs-modules

# Verify smb.conf and disable username map script if present
sudo sed -i '/username map script/d' /etc/samba/smb.conf

# Add restrictive Samba configuration
sudo tee -a /etc/samba/smb.conf > /dev/null <<'EOF'

# Security hardening for Samba 4.x
restrict anonymous = 2
smb encrypt = mandatory
EOF

# Validate smb.conf syntax
sudo testparm

# Start Samba services
sudo systemctl start smbd
sudo systemctl start nmbd
sudo systemctl enable smbd
sudo systemctl enable nmbd

# Block SMB ports at firewall for untrusted networks
sudo ufw deny 139/tcp
sudo ufw deny 445/tcp
# If internal access is required, use: sudo ufw allow from 192.168.x.0/24 to any port 139,445
```

**Verification Commands:**
```bash
# Verify Samba version is 4.18 or later
smbd --version

# Verify smbd and nmbd are running
sudo systemctl is-active smbd
sudo systemctl is-active nmbd

# Verify ports 139 and 445 are listening on the new version
sudo netstat -tlnp | grep -E ':139|:445'
sudo ss -tlnp | grep -E ':139|:445'

# Verify username map script is disabled
sudo grep -i "username map script" /etc/samba/smb.conf || echo "Username map script is disabled"

# Verify restrict anonymous is set
sudo grep -i "restrict anonymous" /etc/samba/smb.conf

# Test SMB configuration
sudo testparm -s
```

---

### Fix: SMB User Enumeration — 35 Accounts Exposed via Null-Session-Adjacent RID Cycling

**Severity:** CRITICAL  
**Effort:** Medium

**Remediation Commands:**
```bash
# Backup current smb.conf
sudo cp /etc/samba/smb.conf /etc/samba/smb.conf.backup.$(date +%s)

# Restrict anonymous access to prevent RID cycling
sudo tee -a /etc/samba/smb.conf > /dev/null <<'EOF'
# Disable null-session RID enumeration
restrict anonymous = 2
guest ok = no
EOF

# Validate configuration
sudo testparm

# Restart Samba services to apply changes
sudo systemctl restart smbd
sudo systemctl restart nmbd

# Disable or remove unnecessary system accounts
# (Identify accounts from pentest report that are not required)
# Example: Disable unused service accounts
sudo usermod -L postgres 2>/dev/null || true  # Lock if not in use
sudo usermod -L mysql 2>/dev/null || true     # Lock if not in use
sudo usermod -L tomcat55 2>/dev/null || true  # Lock if not in use

# Enforce strong password policy
sudo apt-get install -y libpam-pwquality
sudo sed -i 's/^password.*requisite.*pam_pwquality.so.*/password requisite pam_pwquality.so retry=3 minlen=14 dcredit=-1 ucredit=-1 ocredit=-1 lcredit=-1/' /etc/pam.d/common-password

# Set account lockout after failed login attempts (using pam_tally2)
sudo apt-get install -y libpam-cracklib libpam-modules
sudo tee -a /etc/pam.d/common-auth > /dev/null <<'EOF'
auth required pam_tally2.so onerr=fail audit silent deny=5 unlock_time=900
EOF

# Implement monitoring for failed authentication attempts
sudo apt-get install -y aide
sudo aideinit
```

**Verification Commands:**
```bash
# Verify restrict anonymous = 2 is set
sudo grep "restrict anonymous" /etc/samba/smb.conf

# Verify guest access is disabled
sudo grep "guest ok" /etc/samba/smb.conf

# Test that RID enumeration is blocked (should fail)
# Requires enum4linux-ng or similar tool; expected: no user enumeration
enum4linux-ng -U localhost 2>&1 | grep -i "user:" || echo "RID enumeration successfully blocked"

# Verify smb configuration is valid
sudo testparm -s

# Check that unnecessary accounts are locked
sudo passwd -S postgres 2>/dev/null | grep -i "locked\|L" || echo "Verify account status"
sudo passwd -S mysql 2>/dev/null | grep -i "locked\|L" || echo "Verify account status"

# Verify pam_tally2 is enabled for lockout
grep "pam_tally2" /etc/pam.d/common-auth

# Test password policy is enforced
sudo passwd testuser 2>&1 | grep -i "fail\|weak" || echo "Test new user password policy"
```

---

### Fix: Exposed Writable SMB Share — `tmp`

**Severity:** CRITICAL  
**Effort:** Quick

**Remediation Commands:**
```bash
# Backup current smb.conf
sudo cp /etc/samba/smb.conf /etc/samba/smb.conf.backup.$(date +%s)

# Remove the writable tmp share from smb.conf
sudo sed -i '/\[tmp\]/,/^$/d' /etc/samba/smb.conf

# Alternative: If tmp share is operationally required, restrict it severely
# Uncomment and modify the following to replace the removed share:
sudo tee -a /etc/samba/smb.conf > /dev/null <<'EOF'

# If tmp share is required (restricted version)
[tmp_secure]
   path = /tmp
## Exploit Suggestions (Metasploit)

> ⚠️  Exploit suggestions are for reference only — do not run against systems without written authorisation.

| # | Trigger | Metasploit Module | Type |
|---|---------|-------------------|------|
| 1 | `CVE-2011-2523` | `exploit/unix/ftp/vsftpd_234_backdoor` | exploit |
| 2 | `CVE-2007-2447` | `exploit/multi/samba/usermap_script` | exploit |

**Usage example (Metasploit console):**
```
msfconsole
use exploit/unix/ftp/vsftpd_234_backdoor
set RHOSTS <target_ip>
set LHOST <your_ip>
run
```